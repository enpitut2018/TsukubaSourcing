<%= form_with(model: jobs_evaluation, local: true) do |form| %>
  <% if jobs_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jobs_evaluation.errors.count, "error") %> prohibited this jobs_evaluation from being saved:</h2>

      <ul>
      <% jobs_evaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="field">
    <%= form.label :star %>
    <%= form.number_field :star %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
